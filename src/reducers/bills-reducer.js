// import {} from '';

const initialState = {
  bills: null,
  loading: false,
  error: null,
}

export const billsReducer = (state=initialState, action) => {
  switch (action.type) {
    case value:

      break;

    default:
      return state;
  }
}